# Enum Next Actions

- Preserve the current blocked state and avoid further blind edits to live unlock or service scripts.
- If available, acquire an external repair payload, repair USB, backup image, or a sibling working device for artifact comparison.
- If no external artifacts exist, perform a last-chance forensic sweep for remnants in alternate histories, package metadata, or deleted-file residue before declaring a cryptographic dead end.
- If the user authorizes it and the cost is acceptable, evaluate offline key-recovery attempts against `vault.img` as a separate workstream.
