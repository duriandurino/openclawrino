name: webhook
kind: quick-scan
description: Safe webhook endpoint triage with webhook-specific enum and baseline web checks.
steps:
  - phase: enum
    script: enum/web/enum_webhook_basic.sh
    args: ["--target", "{{target}}", "--engagement", "{{engagement}}", "{{safe_flag}}"]
  - phase: vuln
    script: vuln/web/web_baseline.sh
    args: ["--target", "{{target}}", "--engagement", "{{engagement}}", "{{safe_flag}}"]
