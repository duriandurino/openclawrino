# Trigger Examples

Use this skill for requests like:

- "Use OpenCode to build a reusable parser for this scan output."
- "Refactor this scratch script into a cleaner CLI tool with arguments and logging."
- "Plan the smallest useful helper for repeated evidence formatting before we build it."
- "Generate a Python utility to normalize recon results from multiple files."
- "Turn this bash one-liner into a reusable session utility with sane defaults."
- "Use OpenCode to scaffold a report-helper script for this engagement."
- "We need a small coding utility for this phase — use OpenCode instead of hand-writing it."
