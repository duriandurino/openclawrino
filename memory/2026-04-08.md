# Memory — 2026-04-08

- User explicitly set the stable passphrase preference for gog/google automation and future related passphrases to `hatlesswhite`.
- This should be reused consistently for `GOG_KEYRING_PASSWORD` and future re-auth/reset flows unless the user changes it.
- User clarified a durable quick-scan reporting expectation: different webapps should not end with the same stale finding set unless the evidence truly matches. Adaptive quick scan must influence the actual findings emphasis and recommendation mix, not just the narrative wrapper.
- Diagnosed repeated Google publish failures as a gog file-keyring passphrase mismatch loop, not random Google auth breakage. Fixes: stop forcing the wrong fallback passphrase, standardize on `hatlesswhite`, and confirm publishing works when that passphrase is set.
- Cleaned quick-scan clutter by archiving old `engagements/quick-*` folders into `engagements/_archive/quick-scan/`, keeping only the newest verification/live runs active.
- Identified that gateway restarts did not fix the main instability; the bigger issue was long-lived session/context carryover plus workspace/session clutter.
- Fixed the final published report path so adaptive quick-scan context now survives into published Docs/Slides/PDF instead of being flattened into a generic template.
- Fixed the last-mile findings synthesis so adaptive quick scan now changes the actual final finding set for different webapps when evidence differs. Verified divergence on recent live runs:
  - Durino final findings: missing CSP, server banner exposed, missing X-Frame-Options
  - OnlineBookFair final findings: missing CSP, server banner exposed, missing X-Content-Type-Options
- Fixed publish workflow freezing by making report generation save compact `.publish.json` summaries and teaching quick-scan publish to read those summaries instead of scraping giant logs.
- At session close, quick scan/reporting/publishing flow is much healthier: Google publishing works again with `hatlesswhite`, adaptive context reaches published outputs, and findings synthesis is no longer purely stale-template driven.
- Stored a new PlayerV2-related encrypted artifact for later pentest work: `engagements/playerv2-artifacts/inbound/setup.enc` (copied from inbound attachment, detected as `openssl enc'd`, SHA-256 `f9116a632d11c3b5331e5a9983caff074cc89ec7c8c63d5ac770794ec2d41b7b`).
- Ran a safe file-level quick scan on `engagements/playerv2-artifacts/inbound/setup.enc`; confirmed OpenSSL salted format (`Salted__` header), size `8752` bytes, and saved notes to `engagements/playerv2-artifacts/quick-scan-setup-enc/QUICK_SCAN_SETUP_ENC_2026-04-08_1706.md`. No inner payload details can be validated until decryption.
- Durable user preference reinforced again: after every quick scan, send the **published outputs** automatically; a local-only scan summary is incomplete.
# Memory — 2026-04-08

- User explicitly set the stable passphrase preference for gog/google automation and future related passphrases to `hatlesswhite`.
- This should be reused consistently for `GOG_KEYRING_PASSWORD` and future re-auth/reset flows unless the user changes it.
- User clarified a durable quick-scan reporting expectation: different webapps should not end with the same stale finding set unless the evidence truly matches. Adaptive quick scan must influence the actual findings emphasis and recommendation mix, not just the narrative wrapper.
- Diagnosed repeated Google publish failures as a gog file-keyring passphrase mismatch loop, not random Google auth breakage. Fixes: stop forcing the wrong fallback passphrase, standardize on `hatlesswhite`, and confirm publishing works when that passphrase is set.
- Cleaned quick-scan clutter by archiving old `engagements/quick-*` folders into `engagements/_archive/quick-scan/`, keeping only the newest verification/live runs active.
- Identified that gateway restarts did not fix the main instability; the bigger issue was long-lived session/context carryover plus workspace/session clutter.
- Fixed the final published report path so adaptive quick-scan context now survives into published Docs/Slides/PDF instead of being flattened into a generic template.
- Fixed the last-mile findings synthesis so adaptive quick scan now changes the actual final finding set for different webapps when evidence differs. Verified divergence on recent live runs:
  - Durino final findings: missing CSP, server banner exposed, missing X-Frame-Options
  - OnlineBookFair final findings: missing CSP, server banner exposed, missing X-Content-Type-Options
- Fixed publish workflow freezing by making report generation save compact `.publish.json` summaries and teaching quick-scan publish to read those summaries instead of scraping giant logs.
- At session close, quick scan/reporting/publishing flow is much healthier: Google publishing works again with `hatlesswhite`, adaptive context reaches published outputs, and findings synthesis is no longer purely stale-template driven.
- Stored a new PlayerV2-related encrypted artifact for later pentest work: `engagements/playerv2-artifacts/inbound/setup.enc` (copied from inbound attachment, detected as `openssl enc'd`, SHA-256 `f9116a632d11c3b5331e5a9983caff074cc89ec7c8c63d5ac770794ec2d41b7b`).
- Ran a safe file-level quick scan on `engagements/playerv2-artifacts/inbound/setup.enc`; confirmed OpenSSL salted format (`Salted__` header), size `8752` bytes, and saved notes to `engagements/playerv2-artifacts/quick-scan-setup-enc/QUICK_SCAN_SETUP_ENC_2026-04-08_1706.md`. No inner payload details can be validated until decryption.
- Durable user preference reinforced again: after every quick scan, send the **published outputs** automatically; a local-only scan summary is incomplete.
- Durable workflow preference added: for coding-heavy implementation work, prefer OpenCode as the first coding harness by default unless the user explicitly wants another tool.
- Added reusable nested vibe-coding helpers for iterative coding work: `scripts/opencode/reusable/opencode_vibe_loop.py` (single-lane OpenCode turn runner with state/event summaries and OpenClaw notifications) and `scripts/opencode/reusable/opencode_vibe_swarm.py` (named-lane wrapper for swarm-style multi-lane OpenCode workflows). User wants this vibe-coding pattern applicable to main session and sub-agents for surprise dev work or hard pentest support.
- Made report slide decks elastic instead of near-fixed length. PPT generation now scales with findings (more finding-detail slides and multi-slide remediation when needed) so Slides are a better quick visual companion to Docs.
- Durable quick-scan enforcement gap discovered: publishing may succeed, but sending published links back was still too behavior-dependent. Added explicit operator enforcement and helper-based retrieval using `scripts/quick-scan/get_publish_links.py`.
- Durable rule tightened again after a user-caught mistake: quick-scan published links must be pulled from the helper/publish summary, not copied manually from logs or memory. Wrong-link replies can produce false “file does not exist” errors even when publishing succeeded.
- Re-ran `setup.enc` as a proper quick-scan deliverable under `engagements/quick-setup-enc-2026-04-08-1719/` and published it successfully. Correct published outputs came from the publish summary/helper, not the earlier mistaken links.
- User explicitly wants key session context saved before reset, and emphasized that memory should reflect only real, verified work.
- `setup.enc` full engagement round 1 was completed and published under `engagements/playerv2-setup-enc/`. Evidence-backed findings: (1) encrypted setup container blocks pre-deployment security review, High; (2) no companion trust metadata observed with the encrypted artifact, Medium; (3) password-managed installer packaging creates key-handling and recovery risk, Medium.
- `setup.enc` full engagement round 2 was completed and published under `engagements/playerv2-setup-enc-round2/` using the OpenCode vibe-code strategy as a support lane. This round correlated the artifact with adjacent Player/NTV research and pushed into pre-exploit/exploit feasibility.
- Real exploit-phase validation performed for round 2: tested the strongest evidence-backed adjacent key-reuse hypothesis using the previously validated Pi-serial-derived passphrase `ffb6d42807368154` against `engagements/playerv2-artifacts/inbound/setup.enc` in an isolated analysis directory (`/tmp/playerv2-setup-analysis/`) with targeted OpenSSL combinations (`aes-256-cbc` legacy/PBKDF2 and `aes-128-cbc` legacy/PBKDF2). Result: no successful decryption. This is a verified blocked path, not a guessed success.
- Strongest current evidence-backed next step for `setup.enc`: obtain either (1) the true passphrase/key material, or (2) the original script/workflow that generated `setup.enc`. Without one of those, inner installer review remains blocked by encryption.
- Published outputs currently exist for: Durino adaptive quick scan, OnlineBookFair adaptive quick scan, `setup.enc` quick scan, `setup.enc` full engagement round 1, and `setup.enc` full engagement round 2.
- Durable workflow state at reset time: quick scans must be treated as incomplete until published outputs are generated, retrieved via `scripts/quick-scan/get_publish_links.py`, and sent back to the user. Hand-copying links from logs caused at least one wrong-link incident and should not be repeated.
- Durable workflow preference at reset time: OpenCode-first for coding-heavy implementation, with reusable nested vibe-coding helpers available at `scripts/opencode/reusable/opencode_vibe_loop.py` and `scripts/opencode/reusable/opencode_vibe_swarm.py` for main-session or sub-agent swarm use.
