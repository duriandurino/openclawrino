# Bundle Notes

- pandoc was not available in this environment, so the polished bundle was generated as markdown-first deliverables.
- When pandoc is installed, these markdown files can be exported to HTML/PDF/PPTX.
